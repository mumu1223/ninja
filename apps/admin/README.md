# Admin App

Reserved for the future Latesight admin frontend.

Suggested stack:

- Next.js
- TypeScript
- Shared UI from `packages/ui`

This app should consume the Python backend under `services/api`.
