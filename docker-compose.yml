version: "3.8"

services:
  static-site:
    build: .
    container_name: site-ninja
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
    restart: always